[pytest]
markers =
    smoke: critical path tests
    regression: full regression suite
    negative: invalid input tests
    contract: schema validation
    performance: response-time tests

testpaths = tests
python_files = test_*.py
addopts = -q --disable-warnings --maxfail=2
